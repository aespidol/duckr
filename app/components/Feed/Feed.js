import React from 'react';
import PropTypes from 'prop-types';

const Feed = props => {
    return (
        <div>
            Feed
        </div>
    );
};

Feed.propTypes = {
    
};

export default Feed